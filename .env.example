# Root-level shared env. Both ingestion/ and backend/ also have their own .env.example.
# Copy to .env (do not commit). Real values live in the Supabase dashboard and Cohere console.

# Supabase project: Wajha (ap-southeast-1)
SUPABASE_URL=https://uqxvgfvkgwnckkglkauj.supabase.co
SUPABASE_ANON_KEY=                # sb_publishable_... — safe in browser if RLS configured
SUPABASE_SERVICE_ROLE_KEY=        # sb_secret_... — server only, never expose

# Cohere
COHERE_API_KEY=                   # production key from cohere.com
