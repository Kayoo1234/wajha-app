export default function DemoWatermark() {
  return (
    <div
      className="fixed bottom-3 right-3 z-50 select-none rounded-full bg-zinc-900/85 px-3 py-1.5 text-[10px] font-medium tracking-wider text-amber-300 shadow-lg"
      title="Demo prototype — no payment captured, no real orders placed"
    >
      DEMO MODE
    </div>
  );
}
