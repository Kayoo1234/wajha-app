# Backend env. Copy to .env. Do not commit .env.

SUPABASE_URL=https://uqxvgfvkgwnckkglkauj.supabase.co
SUPABASE_SERVICE_ROLE_KEY=        # secret key — server only

COHERE_API_KEY=                   # text embeddings (embed-multilingual-v3)

# Smart Search layer (added 2026-05-17)
GROQ_API_KEY=                     # gsk_... — query intent extraction (Llama 3.3 70B)
GEMINI_API_KEY=                   # AIza... — visual rerank (Gemini 2.5 Flash)

# Backend tuning (optional)
BACKEND_PORT=8000
BACKEND_CORS_ORIGINS=http://localhost:3000
